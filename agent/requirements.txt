# Compatibility entrypoint for tools that expect requirements.txt. Run from
# agent/ (`python -m pip install -r requirements.txt`); requirement paths are
# resolved from the caller's working directory, not this file's directory.
# pyproject.toml remains the single dependency source of truth.
-e .
